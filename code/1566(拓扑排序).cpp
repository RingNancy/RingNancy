#include <bits/stdc++.h>
using namespace std;
const int maxn = 505;
bool mpt[maxn][maxn];  //采用灵界矩阵对图进行存储；
int lev[maxn]; //表示出度；
vector <int> v[maxn];
priority_queue <int, vector<int>, greater<int> > q;//定义一个优先队列；


int main()
{
    return 0;
}